import 'package:flutter/material.dart';

class DestinationView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Destination View'),
      ),
      body: Center(
        child: Text('Welcome to the Destination View!'),
      ),
    );
  }
}
